# Design decisions (active constraints)

Keep only entries that **still constrain Agent / implementation behavior**. History: [backlog/decisions-archive.md](backlog/decisions-archive.md).

**Bar for adding**: only when changing how code is written, API contracts, or query semantics; normal feature delivery goes in `plans/` + `todo.md`.

---

(Add decisions here: dated heading + rationale + rejected alternatives + constraint bullets)
