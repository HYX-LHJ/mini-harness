# code_simplifier — pre-commit refinement archive

When the user asks to **commit/push** and delivery includes business code, Subagent runs `code-simplifier`; Main Agent writes reports here.

**Naming**: `YYYY-MM-DD_topic-summary.md` (date first).

## Report list (new → old)

| Date | File | Scope / summary |
|------|------|-----------------|
| — | — | (no reports yet) |
