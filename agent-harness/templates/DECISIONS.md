# 设计决策记录（活跃约束）

仅保留**仍约束 Agent / 实现行为**的条目。历史内容见 [backlog/decisions-archive.md](backlog/decisions-archive.md)。

**写入门槛**：仅当改变后续写法、API 契约或查库口径时新增；普通功能交付记 `plans/` + `todo.md`。

---

（在此追加决策，格式：日期标题 + 原因 + 否决方案 + 约束 bullet）
