# 测试

本目录存放项目的全部自动化测试。初始化 mini-harness 时创建；新增测试文件一律放在此目录下。

按语言与框架约定组织子目录（例如 `tests/unit/`、`tests/integration/`、`tests/acceptance/`），具体结构写入 `harness/DECISIONS.md` 或项目文档。验收测试应对照 `harness/todo.md` 中的 AC。
