[pytest]
testpaths = harness/tests
pythonpath = .
addopts = -m "not integration"
markers =
    integration: out of gate scope (not collected by default; do not document as required in AGENTS/harness docs)
